group 'org.godotengine'
version '0.0.1'

apply plugin: 'konan'

konan.targets = ['macbook']

konanArtifacts {
    interop('GodotApi')
    dynamic('GDNative') {
        linkerOpts "-framework Cocoa -Wl,-undefined,dynamic_lookup"
        libraries {
            artifact 'GodotApi'
        }
    }
}
